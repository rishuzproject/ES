package com.elecnor.ecosystem.helper;


public class CompanyCalenderHelper {

	/*public CompanyCalendar setApplicableForNextYear(CompanyCalendar holidayCalendar, HttpServletRequest request) {

		if (request.getParameter("isHolidayApplicableForNextYear") == null) {
			holidayCalendar.setIsHolidayApplicableForNextYear(false);
		}
		return holidayCalendar;
	}

	public Long setHolidayIdToDelete(HttpServletRequest request) {

		long holidayId = 0L;
		if (request.getParameter("holidayIdToDel") != null && request.getParameter("holidayIdToDel") != "") {
			holidayId = Long.parseLong(request.getParameter("holidayIdToDel").trim());
		}
		return holidayId;
	}

	public int getYearFromRequest(HttpServletRequest request) {

		int year = 0;
		if (request.getParameter("year") != null && request.getParameter("year") != "") {
			year = Integer.parseInt(request.getParameter("year").trim());
		}
		return year;
	}*/

}
